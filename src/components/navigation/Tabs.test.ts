describe('Tabs Component', () => {
  test('active tab styling is correct', () => {
    expect(true).toBe(true);
  });
  test('onTabPress callback fires', () => {
    expect(true).toBe(true);
  });
});
