describe('NavigationBar Component', () => {
  test('active item styling is correct', () => {
    expect(true).toBe(true);
  });
  test('onItemPress callback fires', () => {
    expect(true).toBe(true);
  });
});
