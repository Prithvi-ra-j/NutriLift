/**
 * Standalone script to seed the database with dummy data
 * Run with: node run-seed.js
 */

// This is a Node.js script, but the seed function uses React Native modules
// So we need to run it through the app or Expo

console.log("⚠️  This script needs to be run through the React Native app.");
console.log("");
console.log("To seed data:");
console.log("1. Open the app");
console.log("2. Go to More tab");
console.log("3. Tap 'Add Dummy Data' button");
console.log("");
console.log("Or use the Expo dev tools console:");
console.log("  import { addDummyWeekData } from './lib/db/seed-dummy-data';");
console.log("  addDummyWeekData();");
